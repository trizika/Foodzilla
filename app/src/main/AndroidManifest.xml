<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.foodzilla">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ReportSpoting"></activity>
        <activity android:name=".SpottingInfo" />
        <activity android:name=".CreateEvent" />
        <activity android:name=".EventDetails" />
        <activity android:name=".RsvpdEvents" />
        <activity android:name=".HostedEvents" />
        <activity android:name=".AvailableEvents" />
        <activity android:name=".MyEvents" />
        <activity android:name=".SpottingMap" />
        <activity android:name=".Registration" />
        <activity android:name=".Spottings" />
        <activity android:name=".Events" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>